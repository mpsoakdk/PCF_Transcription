# Live Transcript Control - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DYNAMICS 365 CUSTOMER SERVICE WORKSPACE              │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    Conversation Form (msdyn_ocsession)            │ │
│  │                                                                   │ │
│  │  ┌─────────────────────────────────────────────────────────┐    │ │
│  │  │       📞 Live Transcript Control (PCF)                  │    │ │
│  │  │                                                          │    │ │
│  │  │  ┌────────────────────────────────────────────────┐    │    │ │
│  │  │  │  Event Listeners                               │    │    │ │
│  │  │  │  ✓ omnichannelTranscriptUpdate (Custom Event) │    │    │ │
│  │  │  │  ✓ window.postMessage                          │    │    │ │
│  │  │  │  ✓ WebAPI polling (optional)                   │    │    │ │
│  │  │  └────────────────────────────────────────────────┘    │    │ │
│  │  │                                                          │    │ │
│  │  │  ┌────────────────────────────────────────────────┐    │    │ │
│  │  │  │  Transcript Display                            │    │    │ │
│  │  │  │  • Agent messages (blue)                       │    │    │ │
│  │  │  │  • Customer messages (gray)                    │    │    │ │
│  │  │  │  • Auto-scroll                                 │    │    │ │
│  │  │  │  • Timestamps                                  │    │    │ │
│  │  │  │  • Sentiment indicators                        │    │    │ │
│  │  │  └────────────────────────────────────────────────┘    │    │ │
│  │  └─────────────────────────────────────────────────────────┘    │ │
│  │                                                                   │ │
│  │  Properties:                                                      │ │
│  │  • conversationId: bound to msdyn_ocsessionid                    │ │
│  │  • maxHeight: 400px                                              │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │
                        Event Flow: CustomEvent / PostMessage
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                    INTEGRATION LAYER (Choose One)                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌────────────────┐         ┌──────────────────┐
│   OPTION 1:   │          │   OPTION 2:    │         │   OPTION 3:      │
│  Web Resource │          │  Browser Ext.  │         │  Backend Service │
└───────────────┘          └────────────────┘         └──────────────────┘
        │                           │                           │
        ▼                           ▼                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  OmnichannelTranscriptPublisher.js                                      │
│  (JavaScript Web Resource on Form)                                      │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  1. DOM Monitoring (MutationObserver)                             │ │
│  │     • Watch transcript panel for new messages                     │ │
│  │     • Extract speaker, text, timestamp                            │ │
│  │     • Publish CustomEvent to window                               │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  2. API Hooks (If Available)                                      │ │
│  │     • Check for Microsoft.Omnichannel                             │ │
│  │     • Subscribe to transcript events                              │ │
│  │     • Relay to PCF control                                        │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  3. Manual API (Testing/Debug)                                    │ │
│  │     • window.OmnichannelTranscriptPublisher.publish()             │ │
│  │     • Manual event publishing                                     │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │
                          Data Source (to be implemented)
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                   OMNICHANNEL / VOICE TRANSCRIPT SOURCE                 │
│                                                                         │
│  Potential Sources:                                                     │
│  • Omnichannel UI transcript panel (DOM)                               │
│  • Microsoft.Omnichannel APIs (if/when available)                      │
│  • Azure Communication Services (ACS) events                           │
│  • Custom backend service with WebSocket/SignalR                       │
│  • Direct integration with voice provider                              │
└─────────────────────────────────────────────────────────────────────────┘
```

## Event Flow Diagram

```
┌──────────────┐
│ Voice Call   │
│   Active     │
└──────┬───────┘
       │
       │ Transcription happening in real-time
       │
       ▼
┌──────────────────────────┐
│  Omnichannel UI          │
│  (Transcript Panel)      │
└──────┬───────────────────┘
       │
       │ DOM updates OR API events
       │
       ▼
┌─────────────────────────────────────┐
│  OmnichannelTranscriptPublisher.js  │
│                                     │
│  1. Detect new transcript           │
│  2. Extract data                    │
│  3. Format event                    │
└──────┬──────────────────────────────┘
       │
       │ window.dispatchEvent('omnichannelTranscriptUpdate', {...})
       │
       ▼
┌─────────────────────────────────────┐
│  LiveTranscriptControl (PCF)        │
│                                     │
│  1. Receive event                   │
│  2. Validate conversation ID        │
│  3. Add to transcript array         │
│  4. Render utterance                │
│  5. Auto-scroll                     │
└──────┬──────────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Agent sees live         │
│  transcript on form      │
└──────────────────────────┘
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────────┐
│                        RUNTIME LIFECYCLE                        │
└─────────────────────────────────────────────────────────────────┘

1. FORM LOAD
   │
   ├─> PCF Control: init()
   │   • Create UI elements
   │   • Get conversation ID
   │   • Subscribe to events
   │   • Set status: "Listening"
   │
   └─> Web Resource: initialize()
       • Get conversation ID
       • Start DOM monitoring
       • Expose manual API

2. TRANSCRIPT UPDATE
   │
   ├─> Omnichannel: New utterance appears
   │
   ├─> Web Resource: Detect change
   │   • Extract speaker
   │   • Extract text
   │   • Extract timestamp
   │   • Publish event
   │
   └─> PCF Control: Receive event
       • Validate conversation ID
       • Add utterance to array
       • Render new message bubble
       • Scroll to bottom

3. FORM CLOSE
   │
   ├─> PCF Control: destroy()
   │   • Remove event listeners
   │   • Stop polling (if enabled)
   │   • Clean up UI
   │
   └─> Web Resource: cleanup()
       • Disconnect MutationObserver
       • Clear processed messages
```

## Data Flow

```
┌─────────────┐
│  Speaker    │──┐
│  "Agent"    │  │
└─────────────┘  │
                 │
┌─────────────┐  │
│  Text       │  │     ┌──────────────────────────────┐
│  "Hello..."│──┼────▶│  OmnichannelTranscriptEvent  │
└─────────────┘  │     │  {                           │
                 │     │    conversationId: "guid",   │
┌─────────────┐  │     │    utterance: {              │
│  Timestamp  │──┼────▶│      speaker: "Agent",       │
│  "2025..."  │  │     │      text: "Hello...",       │
└─────────────┘  │     │      timestamp: "2025..."    │
                 │     │    }                         │
┌─────────────┐  │     │  }                           │
│  Sentiment  │──┘     └──────────────┬───────────────┘
│  "positive" │                       │
└─────────────┘                       │
                                      ▼
                            ┌──────────────────────┐
                            │  CustomEvent         │
                            │  dispatched to       │
                            │  window              │
                            └──────────┬───────────┘
                                      │
                                      ▼
                            ┌──────────────────────┐
                            │  PCF Control         │
                            │  handleCustomEvent() │
                            └──────────┬───────────┘
                                      │
                                      ▼
                            ┌──────────────────────┐
                            │  TranscriptUtterance │
                            │  {                   │
                            │    id: "unique",     │
                            │    speaker: "Agent", │
                            │    text: "Hello...", │
                            │    timestamp: Date,  │
                            │    sentiment: "..."  │
                            │  }                   │
                            └──────────┬───────────┘
                                      │
                                      ▼
                            ┌──────────────────────┐
                            │  renderUtterance()   │
                            │  • Create DOM        │
                            │  • Style             │
                            │  • Append            │
                            │  • Scroll            │
                            └──────────────────────┘
```

## Integration Patterns

### Pattern 1: Direct DOM Monitoring (Current)
```
Omnichannel UI → DOM Changes → MutationObserver → Extract Data → Event → PCF
```

### Pattern 2: Future Official API (When Available)
```
Microsoft.Omnichannel → subscribe() → onTranscript() → Event → PCF
```

### Pattern 3: Backend Service
```
ACS Transcript → Backend → WebSocket → Web Resource → Event → PCF
```

### Pattern 4: Hybrid (Recommended for Production)
```
Multiple Sources → Aggregation Service → Unified Events → PCF
```

## File Dependencies

```
SampleControl/
│
├── ControlManifest.Input.xml
│   └── Defines: conversationId, maxHeight
│
├── index.ts (Main Logic)
│   ├── Imports: ManifestTypes
│   ├── Implements: init(), updateView(), destroy()
│   ├── Handles: Custom events, PostMessage, Polling
│   └── Manages: State, UI rendering, Event cleanup
│
├── css/LiveTranscriptControl.css
│   ├── Styles: Container, header, messages
│   ├── Themes: Agent (blue), Customer (gray)
│   └── Features: Animations, scrollbar, responsive
│
└── generated/ManifestTypes.d.ts
    └── Auto-generated from manifest
```

## Deployment Flow

```
1. Development
   npm install → npm run build → Test in harness

2. Package
   pac solution init → pac solution add-reference → msbuild

3. Deploy
   Import solution → Add to form → Bind properties

4. Configure
   Upload web resource → Add to OnLoad → Publish

5. Test
   Open conversation → Run test script → Verify display

6. Production
   Monitor logs → Gather feedback → Iterate
```

## Key Design Decisions

### 1. Event-Driven Architecture
✅ **Why**: Decouples transcript source from display
✅ **Benefit**: Flexible integration, future-proof

### 2. Multiple Integration Methods
✅ **Why**: No official API available
✅ **Benefit**: Works in different scenarios

### 3. Client-Side Only
✅ **Why**: Minimize latency, no server round-trip
✅ **Benefit**: Real-time performance

### 4. XSS Protection
✅ **Why**: Security best practice
✅ **Benefit**: Safe HTML rendering

### 5. Auto-Scroll
✅ **Why**: Better UX for live updates
✅ **Benefit**: Always see latest message

---

This architecture provides:
- ✅ Flexibility in integration
- ✅ Future-proof design
- ✅ Production-ready quality
- ✅ Extensibility
- ✅ Performance
